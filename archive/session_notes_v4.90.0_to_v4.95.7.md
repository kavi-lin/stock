# Session Notes 歸檔 v4.90.0 → v4.95.7（新在上，共 10 個）

> 由 scripts/rotate_session_notes.py 產出。查特定版本：Grep 版號於 archive/session_notes_*.md。

## 🟢 Session Note (v4.95.7) — limiter 解決爆量，不會替錯誤的 fan-out 設計變快

- **圖上每分鐘約 220 calls 證明 limiter 正常，也直接排除「再調 limiter」這條路。** Upcoming calendar 實測有 2,837 symbols，舊程式先對全部逐檔抓 profile、最後才篩 US $2B+；修後用一次 company-screener bulk snapshot 本機 join，cold cache 2 calls、warm cache 0 calls，仍保留全市場中大型股語意。
- **HTTP 200 不是 completeness gate。** 過去 30 日請求剛好回 4,000 rows，但 min date 只到 7/29；新共用 fetcher 看到 cap 就遞迴切日期區間、邊界去重，單日仍 cap 則 fail closed。8/6 pulse 現有 131 檔 universe 中取得 106 筆有效財報樣本。
- **端到端驗收**：27 tests passed、py_compile rc=0；upcoming 實測 2,844 calendar rows → 313 US $2B+ events；完整 `phase_prefetch` 266.5 秒、九項全 rc=0、`hard_fail=false`，前次 smart-money/earnings-calendar 360 秒 timeout 已消失。

## 🟢 Session Note (v4.95.6) — RSS 的可信度與「離原始事件多近」是兩回事

- **既有 `HIGH` 把 Fed、CNBC、Yahoo 放在同一層，去重無法保證留下原始來源。** 新增獨立 `source_kind` 維度；相同事件先依 official / filing / wire / publisher / aggregator / press-release 排序，再以 credibility 解同類 tie。Digest 與 Break News 共用同一函式，避免兩條 intake 漂移。
- **實測先於加源。** MarketWatch MarketPulse 最新項約 9,555 小時前，直接移除；SEC Press、FTC Competition、Census Indicators、EIA Press 均以現有 parser live 通過。隔離 24h smoke 共 132 筆，其中官方項僅 3 筆，符合低量高權威定位。
- **健康資料原本有產生但在 merge 時被自己蓋掉。** RSS intermediate 與 unified raw 共用檔名，`fetch_all_news.py` 只 snapshot items、遺失 `feed_stats`。現在 snapshot 完整 payload 並把 15 筆 feed metadata 帶入 unified artifact。
- **驗收**：news tests 53 passed、live RSS 15/15 可解析、unified smoke `feed_stats_count=15`、News digest validator rc=0。Break News 全庫 validator 仍被 2026-05-30 legacy artifact 擋；既存 Nexus direct-edge 測試另有 1 fail，皆非本版改動。

## 🟢 Session Note (v4.95.5) — 220 RPM limiter 只管得到有來報到的人

- **8/5 sector 429 不是 20GB bandwidth，而是本機有旁路。** `phase_prefetch.py` 的中央 FMP 工作雖受 220 RPM rolling window 控制，外部 earnings-calendar 卻用 16 threads 對整週財報 symbol 逐一抓 profile；本場光最後成功保留就有 213 symbols，未受控流量與中央流量疊加後可超過 Starter 300 RPM。
- **修法是收斂入口，不是調高 limiter。** prefetch 改用 repo-owned economic/earnings calendar scripts；calendar 與每一個 profile request 全走 `fmp_pool`。fallback 文件也同步換路徑，避免主路修好、偵錯路徑又繞回旁路。
- **429 現在留下可判讀、不可洩密的證據。** `~/.cache_bridge/fmp_last_429.json` 原子保存 endpoint（無 query）、安全化 response body 與 `Retry-After`；backoff 取 exponential 與 Retry-After 較大者。29 tests passed、py_compile 與 diff check rc=0；未用目前 agent process 的舊環境 key 重跑 live FMP。

## 🟢 Session Note (v4.95.4) — primary LLM 終於也是 Dashboard protocol 的 primary

- **根因是兩套路由表面共用設定、實際斷開。** Sidebar 寫入 `config/llm_config.json` 的 primary/secondary/tertiary，但 `run_protocol()` 直接 `proto_model="claude"`，所以盤前檢查中的 news + sector 一律跑 Claude。現改由 `pick_model()` 在啟動時選擇；長 protocol 中途失敗不自動重放到另一 provider，仍以 artifact/validator gate 失敗收尾。
- **Codex 核心能力無阻斷，但外圍原本只懂 Claude JSONL。** CLI 支援 workspace 寫入、shell/web tools 與 collaboration subagents；補上 tool 名稱映射 prompt，並讓 Dashboard 進度、錯誤擷取、token usage 識別 Codex `thread/turn/item` events。
- **驗收**：live config 選到 `codex`，命令為 repo-root writable `codex exec --json --ephemeral`；`test_protocol_model_routing.py` 、`test_model_router_window.py`、`test_heatmap_pe_retry.py` 全 rc=0；未實跑完整盤前 protocol，避免為驗證浪費一次長模型額度。

## 🟢 Session Note (v4.95.3) — 五 agent review 彙整收尾：NaN 是「同構位置」清單上最會躲的那個

- **L5 的 NaN 洞是全輪最危險的一筆，而它的存在方式很說明問題：同一輪工作裡兩支 `_num` 分歧了。** 4.95.1 給 `sector_score_calculator._num` 加了 `math.isfinite`，同版出貨的 `sentiment_score._num` 沒有——NaN market_composite 不走降級分支，`_clamp` 因 `min(hi, nan)` 回 hi 而輸出**上界 +3.0 的假極端看多**且 `degraded_reason=None`，假分數直接流進契約與 shadow 樣本；規則層 NaN 則被讀成「查過了、中性」，還會壓低翻預設判準要看的 missing_inputs 集中度。mutation 驗證錯法分毫不差（`score=3.0 degraded=None`）。同構第三處：`shadow_report._direction(nan)` 實跑回假 `"NEUTRAL"` 進翻轉率樣本，一併補。**判準：「擋 NaN」這種守則要當成 `_num` 的定義的一部分帶著走，不是逐檔想起來才加——兩支同名函式在同一輪分歧，就是靠「各自維護」必然的結局。源頭也要堵（yfinance `shortPercentOfFloat` 偶發 NaN，`float()` 原樣放行）。**
- **flip 判準原本量到的是帶寬，不是方向。** LLM lane score 是整數（±1 起跳，除 0 外必落在 0.5 中性帶外）、det 是連續值——「LLM +1 vs det +0.45」這種溫和同向被記成 direction flip，溫和情緒個股會單靠帶寬把翻轉率頂在 20% 門檻上方，而那個門檻擋的是翻預設。翻轉分子改只數 POS↔NEG 對翻；NEUTRAL↔方向性拆成 `band_mismatch` 另列（它集中出現時該檢討的是 `SENTIMENT_NEUTRAL_BAND` 的值，不是擋翻預設）。**判準：一個「不一致率」判準若混了兩種語意的不一致，先拆開再讀數字——混著讀時任何門檻都同時太鬆又太嚴。**
- **`shadow_only` 大門補上 truthiness 繞道**：validator 舊版 `is False` 才 error，`0`／`"false"` 只拿 warning。改「true 通過、缺欄 warning、其餘一律 error」。與 4.95.2 的 `sample_valid is True` 同一條紀律：守大門的欄位只認 JSON 字面值。
- **`_read_sector_rotation` 從「手抄等價」改成「共用同源」**（抽 `_fred_sector_rotation()` 兩端 import）——4.90.3 那條判準的預防性應用：兩份手抄只在形狀清單不變時等價。其餘 P3 一次收：build 端 list 型 `score_components` 乾淨 die、backfill 基準日進場定格（跨夜 build 不再誤標）、負數計數器判 malformed（可抵銷跨場 sum 的真 hard diff）、兩處落盤改原子寫入、`would_invoke: null` 不再是 skip 票、`--date` 非 ISO 在 argparse 層擋（原本靜默寫出 `fred_lane_gate_8/3/` 子目錄）、`fred_generated_at` 上 `--prompt-only` 路徑、`RULE_LIBRARY` 門檻全常數插值＋測試釘住。
- **刻意不修三筆**：SE1「hard-require die() 是 live 行為變更混在 shadow 集出貨」（既成事實、方向刻意，記錄即可）；SE3b FRED stale 門檻（規格決策，TODO 已有）；L5 契約側（review 全數通過）。backfill 接線的 build 層測試做不了——`build_phase0()` 經 subprocess 讀 live repo cache、無 cache-dir 注入點，hermetic 測試要先重構，已立 TODO（SE1b）。
- **驗收**：7 支測試 rc=0（sector 3 + sentiment + lane_contract + session_export_schema + valuation_reviewer_gate，各自新增回歸案例）；gics 18 passed；`validate_session_export.py` 對真實 history rc=0；`shadow_report.py` 實跑正常；sentiment NaN 修正 mutation 驗證 5 紅；SYNC OK 4.95.3。producer 版號：`sentiment_score` v1.1（NaN 行為變更）、`da_pretrigger` v1.2、`fred_lane_gate` v1.2、`CALCULATOR_VERSION` 續不動。

## 🟢 Session Note (v4.95.2) — 對 4.95.1 的第二輪 review：修 bug class 的那輪自己也漏掃同構位置

- **4.95.1 喊「韌性下沉到欄位層」，但四個地方沒把自己的判準貫徹到底。** (1) SE3 的 `uncovered[]` 修到 key 層就停——`{"Utilities": null}` 或訊號欄位全 null 的板塊，key 在、值判不了，照樣讀成「查過了、乾淨」，**該輪要殺的 bug class 在下一層原樣復發**；(2) SE5 修了 gate 自己的 `_canon_list`，沒修三個消費端共用的 `_slim_fred()` 源頭（favor/avoid 字串/髒元素直通下游）；(3) `_shape_warnings` 蓋了 raise/lower 卻漏了同為判定輸入的 avoid；(4) 讀出端擋了「版號缺失」卻放行「版號非字串」——float 版號讓 `sorted()` 混型 TypeError 直接掛掉 `--status`。**判準（4.90.4 的重演，這次發生在 review 輪自己身上）：修 bug class 的 PR 收尾時，要拿修法那條判準回頭掃 PR 自己新增的程式碼——「往下一層」這個動作本身也有下一層。**
- **rc 契約要守的是「所有輸入」，不是「檔頭點名的那個輸入」。** `fred_lane_gate.py` 的 rc=1 契約寫「只在 fred cache 不可讀」，但 `previous_regime_label()` 讀的歷史 intel 是第二個輸入——壞形狀（頂層 list、`_phase0` 字串、label 非字串）會 AttributeError traceback：rc=1 且 stdout 空，且**一份壞歷史檔會拖死之後每一場 gate**（它永遠在 glob 結果裡）。修成逐層 isinstance、跳過往前找。
- **SE4 抓到的是「文件對、code 錯」的少數派**：`build_sector_intel.py:198` 從 `market_state` 讀 FTD `exposure_range`，欄位實際與 `total_score` 同住 `quality_score`（`post_ftd_monitor.py` 寫入處與 `phase_0.md:72` 都對）——live intel 這欄**恆 null 沒人發現**，因為 null 是它的合法值。修後真實 cache 實跑讀到 `0-25%`。**恆 null 的合法欄位是 doc-vs-code 矛盾的最佳藏身處，值得列入 review checklist。**
- **割接分母的三個灌水口一次補齊**：`.prev` 備份檔被 glob 撈進來同場數兩次（audit 端 4.95.1 就跳過，讀出端漏）、`sample_valid` truthiness 讓字串 `"false"` 進分母、非字串版號炸統計。**`CALCULATOR_VERSION` 刻意不 bump**——本輪沒動計分語意，bump 反而讓混版本檢查把既有 shadow 池標成 mixed、卡住割接分母（版號的語意是「計分邏輯的版」不是「檔案的版」）。
- **驗收**：三支測試新增回歸案例全 rc=0；**revert 模擬逐項紅**（key 層版在 `cover2.null_entry` 炸、淺守衛版 CLI stdout 空、`sorted()` 混型 TypeError、分母 5→7）；`tests/test_gics_sector_audit.py` 18 passed；`test_lane_contract.py` rc=0；`build_phase0()` 唯讀實跑 exposure_range 有值；SYNC OK 4.95.2。
- **未完**：L5（`apply_det_shadow.py` L5 映射）的 review agent 尚未回報，findings 另輪處理。SE1 抽查另留一筆觀察：SE1 的 build 端 gate 用 `is None` 判缺格、calculator 端用 `_num()`——兩端嚴格度不對稱（字串分項過 build gate 但被 shadow 判 invalid），行為安全但語意上「同一格資料兩端給出不同的『可用性』答案」，本輪已讓 da_pretrigger 端把這種型別髒值標 uncovered，build 端 gate 是否收嚴留待 SE 路線。

## 🟢 Session Note (v4.95.1) — SE 四筆的 code review：四支獨立寫的 script 犯了同一個錯

- **四筆共用一個盲點：韌性只做到「檔案在不在」，沒做到「欄位／元素對不對」。** 四支 script 是不同時間分開寫的，卻各自在同一個位置漏同一種守衛：SE1 的 `_num(...) or 0` 讓單一 null 分項變 0 分、SE3 的 `cache.get(s) or {}` 讓缺席板塊變「沒觸發」、SE5 的 `_canon_list` 讓 list 內非字串炸掉。三處都**已經**在整檔層級寫了正確的守衛（`missing_score_components` / `_load_hard_cache` rc=1 / `isinstance(a, dict)`），只是規則沒往下延伸一層。**判準：寫「資料不可用」守衛時，要問的是「不可用的最小單位是什麼」——檔案、板塊、還是欄位。答案幾乎都是最小的那個，而測試會因為現有資料乾淨而全綠。**
- **最糟的是 SE1 那個：假 hard diff 會永久擋死 SE2，而且擋的理由是假的。** 割接判準是**累計** 0 hard diff，一筆 `theme_heat: null` 就產生 `diff=20` 且 `reproducible=True`，混進證據池後 `--status` 永遠不會達標，讀的人還會以為是 LLM 在漂。**「算不出 ≠ 算錯」這條規則 SE1 自己寫得很清楚，只是實作在 dict 缺席的層級，沒實作在欄位層級。**
- **`--audit-decisions` 守住了正門，`--date <舊日期>` 是後門。** 重跑過去日期的 build 會照常落盤進 live 分母，而且 `build_phase0()` 讀的是**今天**的 FRED/breadth cache——存下的 `context.fred_available` 根本不是掃描日的狀態。repo 裡的 `.prev-0101` 備份證明重跑舊日期是實際會發生的操作。**判準：做了 audit/live 分離之後，要把「還有誰能寫進這個池子」列一遍，而不是只守自己新增的那條路徑。**
- **half-up 的修正是「按原則而非按資料」定的，值得記一筆。** 473 筆歷史樣本對四個捨入變體給出**完全相同**的結果（沒有樣本落在邊界），所以資料無法裁決。改用兩個可驗證的事實決定：(a) `round(pre*mult, 2)` 是銀行家捨入，造成雙重捨入（`64.25 × 1.035` → 67 而非 66）；(b) 實掃乘數表 × 0.5 間隔分數，`50.0 × 1.15 == 57.49999999999999` 等 3 例真值恰為 `.5`，裸 `floor(v+0.5)` 會少 1 分——所以 eps 不是保險是必要。**上一版把 `round()` 換成 half-up 是對的，但只換了最外層。**
- **SE4 遺失的那條 rubric 是「被錯誤的分類保護起來」的。** `exposure_ceiling` 被歸進「script 自動填」那句話裡，於是它的映射規則（← `composite.exposure_guidance`）跟著整段被刪——但它其實是 `require(decision, ...)` 硬取的 LLM authored 欄位，缺了整場 build 中止。**文件瘦身的危險不在「搬錯地方」（那看得出來），在「分類錯了於是連帶刪掉」**：`phase_4-5.md` 那份必填 key 清單一直都在，兩份主檔矛盾了一版沒人察覺。
- **驗收**：三支測試 rc=0（各自新增守衛的回歸案例）；`tests/test_gics_sector_audit.py` 18 passed；`validate_sector_intel.py` rc=0；**`--audit-decisions` 43 場數字修前修後完全相同**（38 clean / 4 hard / 33 soft / 1 drift，證明新守衛在真實資料上零新增排除）；7/31 實跑驗證 backfill 判定（`sample_valid=False` + `backfill_rebuild`，`--status` 分母不計）後**已刪 shadow 檔並還原 intel 檔（逐位元比對相同）**；SYNC OK 4.95.1。

## 🟢 Session Note (v4.92.0–4.95.0) — S路線 SE1/SE3/SE5/SE4：三個「我先假設、資料打臉」
- **SE1 動手前的假設被 33 筆歷史樣本推翻，而那個假設會讓 shadow 全面失真。** 我原本判定「calculator 沒模 Step 5 特殊乘數 → binary risk 日會報假 hard diff」，準備補模 ×0.70。實際掃過帶 `binary_risk_within_48h` 的 33 筆，**31 筆**的 `sum(score_components) × step6_mult` 直接等於 `composite_score` —— LLM 是把乘數**烙進四個分項之後**才寫下的（2026-07-31 Energy 的 `key_reasons` 白紙黑字：「已因 OPEC 會議套用 binary ×0.70（91.8 → 64.3）」，64.3 正是四分項的和）。我補模的版本實跑當場就產生 diff=20 的假 hard diff。**判準：協定文字說「乘數套用於 Score_adjusted」不等於 LLM 寫進 JSON 的就是套用前的值——欄位語意要用歷史樣本反查，不能只讀 spec。** 改為「偵測不重算 + 標 `unverified_steps`」。
- **「shadow 跑很久沒事」在這裡是零證據，不是好證據。** 結果只印 stderr、`SECTOR_CALC_STRICT=1` 預設沒人設，所以過去幾個月既沒人看也沒東西可看（7/21、7/29、7/31 三場 intel 皆無 shadow 欄位）。落盤後補了 `--audit-decisions` 重放 43 場歷史 decision（**唯讀、不寫 shadow 樣本**，因為那些場次 `_phase0` 已不可考，混進去就是 C1 說的假證據）——38 場全乾淨，且問題**幾乎全集中在 5 月早期**，7 月以後 9 場裡 8 場全乾淨。SE2 的證據基礎因此一次到位，不必等 5 場。
- **`round()` 是銀行家捨入這件事值一個具名函式。** 473 筆樣本實測 half-up 92.2% vs banker's 91.5%，差的 3 筆全是 `.5` 邊界且 LLM 三次都進位。用錯不會炸，只會產生整批 ±1 的系統性 soft diff——正好落在 soft 門檻內，看起來像「LLM 心算有點飄」而不是「我捨入方式錯了」。已寫成 `_round_half_up()` + 註解 + 測試釘住。
- **SE5 查 trigger 該長什麼樣時，撞到一個既有缺口**：`sector_rotation.adjustments`（GUIDE RULE 1「overrides favor」）**沒有任何腳本消費**——`step6_overlay.py:81-82` 只讀 favor/avoid，`_slim_fred()` 的 11 欄也不含它。也就是 **FRED lane 是唯一執行者，但它收到的 `_phase0.fred_snapshot` 根本沒有這個欄位**——lane 被要求套用一份它從未收到的資料。**本版只記錄不修**：補資料會改 lane 產出＝行為變更，不與 shadow-only 的 gate 同版動。已列進 TODO 的 SE 路線。
- **SE4 的切分準則是「先查誰在用」，不是「看起來像備援就搬」。** `cycle_phase` 推斷規則留主檔（`build_sector_intel.py` 用 `require(decision, ...)`，仍是 LLM authored）、`warning_flags` 表可以走（已 script 化）。舊「欄位映射表」**沒有原樣搬進 appendix 而是標記為非現行 spec**——實查它與實作有三處不一致（`breadth_components` 1 欄 vs 4 欄、`regime_confidence` 硬編 0.9 vs data_quality 映射、`warning_flags` 4 種 vs 6 種）。把過期的表當「參考資料」搬走，等於讓它繼續誤導。常駐 context −157 行。
- **併發注意**：本 session 進行中 `VERSION` 從 4.90.3 被另一 session 推到 4.91.0（L5，檔案在 `skills/market-sentiment-analyzer/`）。四項因此順延為 4.92.0–4.95.0，程式內嵌版號一併校正。**同工作目錄有並行 session 時，版號要在收尾前重讀一次，不能沿用開場讀到的值。**
- **驗收**：三支新測試 rc=0（`test_sector_score_calculator.py` / `test_da_pretrigger.py` / `test_fred_lane_gate.py`）；`tests/test_gics_sector_audit.py` 18 passed；`validate_sector_intel.py` rc=0；7/31 cache 實跑 build → shadow 落盤 `sample_valid=True status=PASS`（Energy 正確標 unverified）並**還原歷史檔**；`da_pretrigger` 輸出與當日 intel 的 `sector_divergence_watch` 逐字吻合；SYNC OK 4.95.0。

## 🟢 Session Note (v4.91.0) — L5 Sentiment det producer（backlog 說「已寫死」，實際只寫死一半）
- **TODO 條目寫「公式已寫死（0.5×stock + 0.5×(market/10−5) + 規則表）」，動工盤點發現只有市場層是真的。** 市場層可逐位元驗證——PLTR 2026-08-02 的報告寫 `0.5 × (-1.5) + 0.5 × (+0.92) = -0.29`，history 存的正是 -0.29，`+0.92` 回推 composite=59.2 與今天實跑 `sentiment.py` 一致。但 `stock_specific` 全庫 grep 只命中 protocol 那一行公式與報告裡的敘述值，**加總方式與 clamp 從未定義**。規則表給的是「哪個訊號幾分」，沒說怎麼合成一個數。**判準：backlog 說「公式已寫死」時，要分清楚寫死的是『母公式』還是『可執行的規格』——前者只保證形狀，後者才能 script 化。**（同型於 4.87.0 記的「條目描述的是動工前的理解」，但這次過期的不是理解而是**規格完整度的評估**。）
- **填空白 vs 憑空發明的界線，我用「有沒有既有定義可沿用」劃。** 三個定案裡只有 insider 取 `quarters[0]` 是**沿用**（schema 既有的 `det_inputs.insider_ratio_q` 就是這個定義）；聚合=加總、clamp=[-3,+3] 是**填空白**——後者寫成具名常數 + 測試釘住 + protocol 表格列出，讓「這是誰定的」在檔案裡看得見。clamp 那條有 69 筆歷史當旁證（lane score 全落在 [-3,+3]，代表 LLM 一直在 clamp，只是沒人寫下來）。L2 當時拒做 Trader 觸發條件是因為**母公式都不存在**，這裡差別在此。
- **det 化會移除 LLM 的規則表外因子，而那正是要量的東西。** 歷史報告看得到 LLM 把表外項目算進 `stock_specific`（APP 那筆的「retail/momo 資金 5 日 +19% → ≈+1」）。所以哨兵看**方向翻轉率**（`SENTIMENT_DIRECTION_FLIP_MAX=20%`）而不是分數差——連續值有落差是設計預期，決策層真正感覺得到的是方向翻掉。
- **契約側只映 `score`，不映 `producer_version`。** 這格最容易寫錯：契約的 `producer_version` 回答的是「這條 lane 是誰產的」，shadow 期的答案是 LLM（`protocol:<VERSION>`）。把 det script 版號填進去等於向 Phase 6 宣稱這筆已是 script 產的，分層會把 shadow 期的樣本歸進 script 池——**C1 存在的唯一理由就是這個分層，寫錯這一格等於白做 C1**。det 公式版號與 input_hash 完整留在 `sentiment_det` block。有測試釘住。
- **兩條規則現行資料源做不出來就記缺，不猜**：`institutional.accumulation_signal`（FMP_SUPP_BUNDLE 的 `institutional` block 實測是空 dict）、`SBC > 15% revenue`（bundle 只有 CEO comp）。`missing_inputs[]` 與「規則命中但給 0 分」在總分上都是 0 但語意相反，測試分開鎖。
- **驗收**：`test_sentiment_score.py` rc=0（市場層 golden + 規則表 11 條 + 4 個邊界 + 兩個 clamp 兩端 + 降級 + hash）；`test_lane_contract.py` 新增 sentiment_det 映射 8 條 rc=0；live NVDA 實跑 det score -0.04（market +0.92 / stock -1.0，命中 insider_ratio_low + mspr_negative + short_low）；e2e 走真 CLI 驗 `shadow_only=false` → rc=1、block↔契約不一致 → warning；哨兵用合成樣本驗方向翻轉偵測；SYNC OK 4.91.0。
- **未驗**：history 尚無 V5.3 entry，所以哨兵 n=0，det vs LLM 的**真實**分歧一筆都還沒有。翻預設（L5b）要等 20 個 session；在那之前值得先看 `missing_inputs` 是否集中在同幾檔——若是，補資料源比翻預設優先。

## 🟢 Session Note (v4.90.0) — C1 統一 lane 資料契約（契約的價值在保留規則，不在形狀）
- **這塊最容易壞的地方不是形狀，是「被自己的 post-processor 覆寫」。** 契約值域寫錯會被 validator 擋，但 `build_lane_contract()` 若無條件重算，L5/L6/L8 在自己 phase 寫好的 `provenance: "deterministic"` 會在 Step 1.5 被靜默改回 `llm` 預設——契約還在、值域還對、validator 全綠，只是每個 lane 都變成 LLM 產出，而 Phase 6 就是照這個欄位分層的。所以 producer 的規則是「只補 None 的欄」，`test_lane_contract.py` 的保留 case 也最多（含 `llm_invoked=False` 與 `shadow_score=0.0` 這兩個 falsy-but-not-empty 的坑）。**判準：一個純 metadata 欄位的失效模式通常是靜默的，測試要照著「壞掉之後看起來仍然正常」去設計。**
- **`apply_det_shadow.py --inplace history.json` 會掃過全部 181 筆，不是只有末筆。** protocol 的 Step 1.5 說明寫「寫入最新一筆」，但 code 是 `for entry in payload`。det_shadow 這樣做無害（冪等），契約不行——給 V4.6 的四 lane session 補一份六 lane provenance 就是在稽核軌跡放假證據。所以 producer 收 `entry_version`，只寫 `V5.3+`。**判準：新增一個「事後補寫」的欄位之前，先確認那支 post-processor 實際掃過的範圍，而不是它文件裡寫的範圍。**
- **red_team 當第六個 lane，不是另立欄位。** L8 條目原本規劃 `red_team_provenance: deterministic_skip`。RT 併進同一張表之後，L8 只要改 `lanes.red_team.provenance` —— 另開平行欄位正是 C1 要消滅的東西。TODO 的 L8 已改。
- **「勿兩套並存」的實作解**：`det_shadow.valuation_score_det` 有五個現存消費端（shadow_report / bridge / Dashboard / 兩支測試），硬搬會炸一片。做法是**同一次計算寫兩處**（不是兩條計算路徑），validator §15 硬性比對相等 —— 自然漂移不可能，不等只可能是有人手改。舊欄降為別名，等契約全面上線再落日。
- **原計畫「renderer 三處相容碼在 C1 落地後刪」我沒做，因為那跟「舊 entry 豁免」互相矛盾。** 形狀鎖只對 V5.3+ 生效，而 renderer 讀得到 181 筆 V5.0 以下的 entry；刪了相容碼＝渲染舊 entry 時靜默掉字。刪除的正確條件是「舊 entry 淡出 render 路徑」，不是「C1 落地」。**判準：規劃階段寫下的清理動作，落地時要用實際的資料分布重新驗一次前提。**
- **e2e 實跑逼出一個我自己種下的偽造面。** 把 FULL EXAMPLE 走一次真 CLI（PM 寫完 → `apply_det_shadow --inplace` → validator）之後，契約長出 `llm_skipped_lanes: [fundamentals, sentiment, news, technical]` —— 因為那份範例從來沒有 `lane_scores`，而我用「有沒有分數」推導 `absent`。protocol 從 V2.10.0 就寫 `lane_scores` 必填，validator 卻從未擋過；於是「少寫一個欄位」＝ 讓四個跑過的 lane 自稱沒產出，事後從 entry 完全看不出來。修法是 V5.3 起硬性要求 `lane_scores`，並把範例補齊（值與 `calculation_steps` 五段乘積一致）。**判準：新欄位若由既有欄位推導，要先確認那個既有欄位真的被強制——protocol 寫「必填」不等於 validator 擋得住。**
- **順手補的下游洞**：Dashboard `VERSION_COLOR` / tooltip 缺 `V5.2`（L2 當時就漏了）與 `V5.3`，新 entry 會落到 LEGACY 的灰色 `ARCHIVE` badge——最新的 entry 被標成最舊的。
- **驗收**：`test_lane_contract.py` rc=0（producer 6 組 + validator §15 tamper 19 例）；e2e 走真 CLI（含一筆 V5.0 混在同檔，確認不回填）；`test_session_export_schema.py` / `test_valuation_pack_consistency.py` / `test_decision_engine.py` / `test_trade_plan_builder.py` / `test_render_investment_report.py` / `validate_session_export.py` 全 rc=0；`shadow_report.py` / `replay_decision_engine.py` / `validate_v219.py` 照跑 rc=0；SYNC OK 4.90.0。
- **未驗**：history 目前**沒有任何 V5.1/V5.2/V5.3 entry**（181 筆全在 V5.0 以下），所以 §15 與 §2e 只在 fixture 上驗過，真實 export 路徑要等下一次 `分析 [TICKER]` session 才會第一次走到。同理 `input_hash` 恆 null、五個非 valuation lane 的 `shadow_score` 恆 null，要等 L9 / L5 才有值。
- **（4.90.1 review 修正）我把保留規則套到了不該套的欄位上，而且測試把錯的行為鎖了進去。** `lanes.valuation.shadow_score` 的 producer 就是 post-processor 自己，`det_shadow` 那塊又每次整塊重建 —— 對它套「非 None 就不動」等於自己的舊值擋住自己的新值：上游 pack 一改就兩邊分歧、§15 吸收閘**永久 rc=1**，而我寫的錯誤訊息叫人「重跑 `apply_det_shadow.py`」根本修不好，唯一出路是手改契約，正是那道閘要禁止的事。**判準：套保留規則之前要先問「這個欄位是誰產的」——保留只對『外來 producer 先寫好的欄位』成立；同一個 producer 自己的欄位，重算永遠該贏。**
  - 修法必須是**無條件**同步，不能是「`val_det` 非 None 才覆寫」：`val_det` 變回 None（pack/fvs 都沒了）時，契約守著舊的非 None 值會留下同一個死結。complementary 的一條：其餘五個 lane 的保留規則完全不動，那條界線現在有測試釘著。
  - **測試把錯的行為當契約鎖住，比 bug 本身更值得記。** 原本那條 `既有 shadow_score 勝過傳入的 val_det` 是我照著「保留規則」的直覺寫的，fixture 還特地捏了一個 valuation 帶外來 `shadow_score` 的狀態——而那個狀態天生過不了 validator。**寫測試時若 fixture 需要捏一個現實中不會出現的狀態才能成立，那多半是在驗證一個不該存在的行為。**
- **（4.90.2 自審）§15 只驗了契約的內部自洽，沒驗契約對外部事實——兩條繞道實跑全綠。** (A) `lanes.sentiment: null` + session 清單配合列進 skipped → 五欄檢查整組被 `if blk is None: continue` 跳過（註解宣稱 absent_lanes 報過，但那只抓缺 key 不抓 null 值）；(B) 有分數的 lane 手改 `provenance: "absent"` → llm_invoked 一致、清單對齊，全部通過——而 Phase 6 分層會把這筆從 LLM 池靜默剔除，正是 C1 要防的 selection bias。修法：per-lane 檢查改為「key 不在才跳過、非 dict 一律 error」；provenance 雙向錨在偽造者改不動的東西上（lane_scores 受 §13 算術鏈保護、`red_team_execution_failed` 是 schema 必填），推導**與 producer 共用同一條** `compute_polarization().missing_lanes`，兩邊不可能因實作分歧吵架。**判準：一致性檢查若只比對「偽造者可以一起改的欄位」，改得夠齊就能過閘；至少一端要錨在他改不動的外部事實上。**
- **（4.90.3 複審 4.90.2 自己）我在修漂移面的那次修補裡，親手引入一個漂移面。** 4.90.2 的註解寫「validator 用**同一條**推導」——`compute_polarization` 確實是共用的，但**它的輸入我重寫了一份**（authoritative valuation score 的取值順序）。producer 哪天改取值來源，validator 這份不跟著動，就會對合法 entry 報 rc=1。抽成 `authoritative_valuation_score()` 兩端 import 才算數。**判準：說「共用同一條推導」之前，把那條推導的『輸入』也數進去——共用一個函式但各自餵不同來源的參數，不是共用。**（抄過去時還漏了 `bool` 排除，`isinstance(True, int)` 為真——重寫一份的代價從來不只是「未來會漂」。）
- **同一輪抓到 4.90.0 那個洞的零售版**：`lane_scores` 只擋了「整塊省略」，`{"fundamentals": 4}` 這種部分省略照樣把三個 lane 推成 `absent`，而 producer 與 validator **一致同意** → 全綠。**「兩邊算出來一樣」不是保護，當兩邊讀的是同一份被動過手腳的輸入時，它只證明兩邊都被騙了。**四個 key 現在必須都在，沒產出的明寫 `null`——與契約「六個 lane 全列、缺席明寫 absent」同一條紀律，只是往上游推一層。
- **（4.90.4，第四輪 code review）修 P2 時我對症不對病——同一個病三天後在旁邊一格復發。** P2 的修法只把「同 producer 重算贏」用在出事的 `valuation.shadow_score` 上；這輪拿同一判準**逐欄位重掃整張表**，發現 `provenance` 的 `llm`/`absent` 與 `llm_invoked` 一樣是 post-processor 自己的推導、一樣被保留規則鎖住，而且**真實操作序列就會踩到**（PM 漏填 lane score → 推成 absent → 補分數重跑 → 上一輪的 absent 被自己保留住 → §15 的「重跑可修」變謊話）。修法是把界線寫成常數 `EXTERNAL_PROVENANCE`：保留域=外來聲明（det/hybrid + producer_version/input_hash/非 val shadow_score），推導域=自己的輸出（llm/absent、llm_invoked 投影、val shadow_score），L5 shadow 形態（provenance=llm + 外來 shadow_score）有測試釘住。**判準：修一個「規則套錯對象」的 bug 時，不能只把出事的那個對象搬走——要拿同一條判準把規則覆蓋的每個對象重新分類一遍，否則同型 bug 就躺在下一格等著。**
